<div class="calendar">
  <% (@end_date - @start_date).to_i.times do |n| %>
    <%= render 'day', date: (@start_date + n.days), row: n %>
  <% end %>

  <%= render partial: 'event', collection: @events %>
